# cs5300

HW1:  
In HW1 We created the Repository, Solved 7 Tasks of variang topics, and created test cases for all of them.    
For HW 1 all Tasks were done in seperate files, however all the tests were done in one specific test file called "test_sample.py".
This file can be run by writing the comand  
Run <b>"pytest test_sample.py"</b> to just run the tests  
Run <b>"pytest --collect-only" </b> to ensure all tests are detected.  
Run <b>"pytest -v"</b> to verify all tests pass.  
