# cs5300

HW1:
For HW 1 all Tasks were done in seperate files, however all the tests were done in one specific test file called "test_sample.py".
This file can be run by writing the comand <\n>
Run <b>"pytest test_sample.py"</b> to just run the tests </n>
Run <b>"pytest --collect-only" </b> to ensure all tests are detected.</n>
Run <b>"pytest -v"</b> to verify all tests pass.</n>
